meta:
  environment: node
  toolchain:
    name: npm
    version: 18

run:
  command: node -r ./max-bootstrap.js -r ./robokassa-bootstrap.js server.js
  persistenceMount: /data
  containerPort: 10000
