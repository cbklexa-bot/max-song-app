meta:
  environment: node
  toolchain:
    name: npm
    version: 18

run:
  command: node -r ./hard-audio.js -r ./max-bootstrap.js -r ./audio-final.js server.js
  persistenceMount: /data
  containerPort: 10000
